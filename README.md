# MDR Review Prototype

A static prototype for a future-state MDR review system, built with React and Vite.

## Local development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Deploy

This repository is configured to deploy automatically to GitHub Pages from the `main` branch using GitHub Actions.
